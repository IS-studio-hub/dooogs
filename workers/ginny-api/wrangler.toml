name = "ginny-dooogs-api"
main = "worker.js"
compatibility_date = "2024-09-23"

# Free Llama chat via Cloudflare Workers AI (no OpenAI key).
[ai]
binding = "AI"

# Optional: point at a public Ollama host instead of Workers AI
#   npx wrangler secret put OLLAMA_BASE_URL
#   npx wrangler secret put OLLAMA_CHAT_MODEL
