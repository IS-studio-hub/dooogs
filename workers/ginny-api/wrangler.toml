name = "ginny-dooogs-api"
main = "worker.js"
compatibility_date = "2024-09-23"

# Set secret (do not commit the key):
#   printf '%s' "$OPENAI_API_KEY" | npx wrangler secret put OPENAI_API_KEY
#
# Optional model override via dashboard or:
#   npx wrangler secret put OPENAI_CHAT_MODEL
